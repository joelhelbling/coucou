# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: coucou
    main: .
    binary: coucou
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/joelhelbling/coucou/internal/cli.Version={{.Version}}

archives:
  - id: coucou
    formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    files:
      - README.md
      - LICENSE*

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^ci:'
      - Merge pull request
      - Merge branch

homebrew_casks:
  - name: coucou
    ids:
      - coucou
    binaries:
      - coucou
    homepage: "https://github.com/joelhelbling/coucou"
    description: "A project-scoped TUI task scheduler for shell commands"
    repository:
      owner: joelhelbling
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com
    commit_msg_template: "Brew cask update for {{ .ProjectName }} version {{ .Tag }}"
    # Our binaries are unsigned, so macOS quarantines them on download and
    # Gatekeeper refuses to run coucou. This strips the quarantine attribute.
    #
    # Homebrew 7.0.4 deprecated `postflight` in favour of `postflight_steps`,
    # so `brew doctor` warns about the generated cask and blames our tap. We
    # cannot fix that here: this setting supplies only the hook *body*, while
    # the `postflight do` wrapper is hardcoded in GoReleaser's own template
    # (internal/pipe/cask/templates/cask.rb). Tracked upstream in
    # goreleaser/goreleaser#6870; the fix is slated for GoReleaser v2.19.
    # Our release workflow pins `~> v2`, so the first release cut after v2.19
    # ships will emit `postflight_steps` and the warning will clear itself.
    # Nothing to do here until then.
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/coucou"]
          end

release:
  github:
    owner: joelhelbling
    name: coucou
  draft: false
  prerelease: auto
  name_template: "{{.Tag}}"
